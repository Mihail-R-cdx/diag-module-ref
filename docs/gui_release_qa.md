# Финальная приёмка GUI

Дата проверки: 29 июня 2026 года.

## Проверенная версия и окружение

- Ветка `master`, `HEAD` `516c750c6d39fbb78738f3625e62b43c60772ec8`
  (`Unify GUI operation states`).
- Проверялось текущее рабочее дерево с незакоммиченными результатами этапа 8
  и финальными QA-артефактами. Коммит не создавался.
- Windows Server 2019 (`10.0.17763`), Python `3.12.9`, Qt `5.15.2`,
  PyQt `5.15.11`, стиль `Fusion`.
- Обязательный путь `C:\Program Files\anaconda3\python.exe` отсутствует.
  Использован доступный
  `C:\Users\Mih\AppData\Local\Programs\Python\Python312\python.exe`.
- Реальные устройства, credentials и сетевые команды не использовались.

## Список проверок

- Нативный запуск `main.py` отдельным процессом и штатная инициализация окна.
- Импорт всех GUI-модулей и компиляция `main.py`, `gui`, `core`, `handlers`,
  `tests`.
- Выбор всех семи поддерживаемых моделей и соответствие экранов:
  Codec, Matrix, PDU и Audio DSP.
- Верхняя панель, placeholder, нижний постоянный статус и время обновления.
- Представительные данные для Huawei TE-40, Extron IN1804, Aten PE8208AV и
  Biamp Tesira Forte CI.
- Пароль, терминалы отладки и таблица журнала звонков без обращения к сети.
- Все общие состояния: idle, loading, connected, disconnected,
  authentication error, request error, unavailable, sleeping,
  command in progress и disabled.
- Сигналы Codec, Matrix и PDU, таймеры, защита от поздних результатов worker
  и публичные `update_data()` API.
- Размеры `800 × 700`, `800 × 800`, `950 × 950`, `1200 × 900`,
  `1536 × 1024`; отсутствие пересечений и горизонтального переполнения.
- Визуальное сравнение с `ToBeGUI.png`: палитра, поверхности, карточки,
  иерархия, primary/active actions и постоянный статус.
- Проверка Git diff: обработчики устройств, протоколы и credentials в рамках
  этапов 8–9 не изменялись.

## Результаты автоматических тестов

Команда:

```powershell
$env:QT_QPA_PLATFORM='offscreen'
& 'C:\Users\Mih\AppData\Local\Programs\Python\Python312\python.exe' `
  -m unittest discover -s tests -p 'test_*.py'
```

Результат: `33` теста, `OK`, время `0.943 s`.

Дополнительно успешно выполнены `compileall` и импорт всех GUI-модулей.
`git diff --check` не выявил whitespace errors; выводит только предупреждения
Git о будущей нормализации LF/CRLF.

Добавлен `tests/test_release_ui_qa.py`, который без оборудования проверяет
все selectable devices, representative payloads, общие состояния и создание
диалогов.

## Результаты ручного smoke

- `main.py` запущен нативно как отдельный Windows-процесс. Через три секунды
  после запуска процесс оставался активен, после проверки был закрыт.
- На реальном Qt-плагине `windows` текст, кириллица, карточки, границы,
  кнопки, статусы и прокрутка отображаются корректно.
- Создан снимок Codec с тестовыми данными в размере `950 × 950`:
  `docs/gui_release_qa_950.png`.
- Компоновка соответствует референсу по визуальному языку, оставаясь
  адаптированной к почти квадратному окну. Длинный Codec доступен через
  вертикальную прокрутку.
- Qt-плагин `offscreen` на этой машине не отрисовал glyphs в screenshot,
  хотя геометрические тесты прошли. Нативный app-only capture подтвердил,
  что это ограничение тестового плагина, а не дефект приложения.

## Исправленные дефекты

На финальном этапе новых локальных дефектов product-кода не выявлено.
Подтверждены исправления этапа 8: минимальный размер, компактная верхняя
панель, scroll areas Matrix/PDU, ограничение ширины значений, перестроение
колонок Codec, контраст, порядок фокуса и размеры диалогов.

Финальный этап добавил недостающее regression-покрытие release-сценариев.

## Известные ограничения и блокеры

### Блокер релиза

Требование об отсутствии GUI-операций, блокирующих event loop, не выполнено.
Несколько пользовательских действий синхронно выполняют подключение или
сетевой запрос в GUI thread:

- журнал звонков: `gui/screens/codec_screen.py:421`;
- создание/подключение codec handler: `gui/screens/codec_screen.py:1274`;
- подключение и переключение Matrix:
  `gui/screens/matrix_screen.py:254`, `gui/screens/matrix_screen.py:307`,
  `gui/main_window.py:721`;
- управление PDU: `gui/screens/pdu_screen.py:261`,
  `gui/main_window.py:1370`.

Медленное или недоступное устройство способно заморозить окно. Безопасное
исправление требует переноса операций в workers, привязки результата к
request identity и корректного cleanup; это не локальная финальная правка.

### Прочие ограничения

- Реальная совместимость команд, таймаутов и ответов оборудования не
  проверена без явного разрешения на подключение.
- Проверен системный масштаб 125%; другие DPI покрыты только layout-тестами.
- `CallLogWindow.load_from_device()` остаётся placeholder, но рабочий путь
  журнала идёт через `CodecScreen.open_call_log_window()`.
- В checklist плана этапы 2, 3, 5 и 6 формально не отмечены, хотя
  соответствующие реализации присутствуют в истории Git. Этап 9 оставлен
  неотмеченным из-за блокера event loop.

## Сценарии, требующие реального оборудования

- Получение данных всех моделей и проверка auth/request/disconnected/sleeping.
- Huawei TE-20: polling, wake, monitor audio, presentation и журнал звонков.
- Huawei TE-40, Bar 310 и Polycom: SIP fix, volume/mute, presentation,
  call log и восстановление после ошибок.
- Extron IN1804: persistent SSH session, routing, status refresh и keepalive.
- Aten PE8208AV: on/off/reboot всех розеток и повторное чтение состояния.
- Biamp Tesira Forte CI: подписки и обновление signal sources.
- Проверка закрытия окна и смены модели во время активных запросов.

## Итоговое решение

**Не готово.** Функциональный hardware-free и визуальный smoke успешны,
но синхронные сетевые операции в GUI thread являются критическим
нерешённым блокером обязательной приёмки. Этап 9 в плане не отмечен.
