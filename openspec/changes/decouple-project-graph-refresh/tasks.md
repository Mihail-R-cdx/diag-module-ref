# Tasks: Decouple project graph refresh

## 1. Governance and ordinary workflow

- [x] 1.1 Update `RULES.md` so ordinary OpenSpec architecture, implementation, independent validation, archive, post-archive checks, and merge do not require Graphify refresh.
- [x] 1.2 State explicitly that an absent or stale graph does not block ordinary validation, archive, post-archive checks, or merge and does not require a change-specific stale-graph exception.
- [x] 1.3 Remove ordinary-change requirements for renewed validation solely for Graphify, validation-report-only commits, Graphify evidence JSON, evidence-only commits, and graph-only commits.
- [x] 1.4 Preserve the authority order and the requirement to verify Graphify-derived hypotheses in current source and tests.
- [x] 1.5 After architecture `APPROVE`, close PR #17 without merge and retain its branch for forensic/reference use.

## 2. Repository-local wrapper

- [x] 2.1 Make `FullRebuild` the default graph publication mode and retain `Incremental` only as an optional mode under the approved preconditions.
- [x] 2.2 Remove publishing mode `Initial` and caller-controlled `BaselineStage`.
- [x] 2.3 Remove `PostArchiveValidationEvidence`, verification-report parsing, post-archive evidence JSON parsing, archive-state checks, and feature-change lineage checks.
- [x] 2.4 Derive `indexed_source_commit` from clean `SourceRoot HEAD` and require explicit `SourceRef` to resolve to the same full SHA `S`.
- [x] 2.5 Require `SourceRoot` and `OutputRoot` to be distinct Git worktrees, both clean and both at `HEAD = S` before generation.
- [x] 2.6 Require the output branch to be created from `S`, reject any pre-existing output diff, and reject any non-allowlisted output mutation before commit.
- [x] 2.7 Read `.graphifyignore` from `SourceRoot`, verify its working bytes against committed `S:.graphifyignore` after repository filters, and do not trust differing output-worktree policy bytes.
- [x] 2.8 Remove hard-coded application file, node-ID, class/function, and concrete edge smoke queries from the blocking publication gate.
- [x] 2.9 Retain only application-agnostic blocking checks for schema, current corpus, internal references, nonzero counts, paths, topology, rename/delete, ghost nodes, security, hashes, and safe publication.
- [x] 2.10 Permit optional application-specific queries only as explicitly non-blocking diagnostic observations.
- [x] 2.11 Publish new maintenance baselines with `baseline_stage = final`, `indexed_source_ref = origin/master`, and `target_branch = master`.
- [x] 2.12 Require all four canonical artifacts to exist and validate in the resulting tree while allowing `S..G` to change only a non-empty subset of the four allowlisted paths.
- [x] 2.13 Do not artificially mutate canonical artifacts whose regenerated bytes are identical to the bytes at `S`; do not create an artificial graph commit when every artifact is byte-identical.
- [x] 2.14 Preserve exact Graphify version pinning, code-only corpus, canonical UTF-8/LF output, valid JSON, nonzero counts, published-byte hashes, and repository-relative paths.
- [x] 2.15 Preserve secret, credential, inventory, absolute-path, user-specific path, unexpected-file, HTML, hook, watch, MCP, skill, and merge-driver rejection.
- [x] 2.16 Preserve safe backup-and-restore publication so failed generation leaves the previous accepted baseline intact.
- [x] 2.17 Preserve optional incremental rename/delete, ghost-node, topology, and integrity checks and force `FullRebuild` when their preconditions fail.
- [x] 2.18 Keep `InstallExact` only if it remains a non-publishing pinned-tool helper.

## 3. Specifications and documentation

- [x] 3.1 Reconcile root `agent-project-navigation` requirements with non-blocking stale graph behavior and separate maintenance refresh.
- [ ] 3.2 Add root `project-graph-refresh-workflow` requirements during archive.
- [x] 3.3 Rewrite `docs/project-graph-runbook.md` around the separate `S -> G` maintenance workflow.
- [x] 3.4 Document the exact maintenance command and the requirement for distinct clean source/output worktrees both based on `S`.
- [x] 3.5 Document committed-source `.graphifyignore` authority and rejection of caller-controlled output policy bytes.
- [x] 3.6 Document tree presence of all four canonical artifacts separately from the non-empty allowlisted-subset `S..G` diff contract.
- [x] 3.7 Document application-agnostic publication checks and removal of historical application-specific smoke queries from blocking authority.
- [x] 3.8 Document `FullRebuild` as standard and `Incremental` as optional under unchanged version, ignore, encoding, source-root, and package-boundary policy.
- [x] 3.9 Preserve the historical frozen baseline and `A -> S -> E -> G` only as forensic history, not a supported future workflow.

## 4. Focused workflow tests

- [x] 4.1 Add positive coverage for `FullRebuild` from one clean exact `origin/master` source SHA without archive or evidence inputs.
- [x] 4.2 Verify `SourceRoot` and `OutputRoot` are distinct, clean, and both initially at `HEAD = S`.
- [x] 4.3 Add negative coverage for same source/output worktree, dirty source, dirty output, output `HEAD != S`, pre-existing output diff, unresolved/mismatched source ref, invalid target, and unexpected source mutation.
- [x] 4.4 Verify `.graphifyignore` is read from `SourceRoot`, matches committed `S` bytes after repository filters, and cannot be replaced by differing `OutputRoot` bytes.
- [x] 4.5 Verify published metadata binds `baseline_stage = final`, `indexed_source_commit = S`, `indexed_source_ref = origin/master`, and `target_branch = master`.
- [x] 4.6 Add a positive structural-refactor case where a formerly hard-coded smoke file/node is moved or removed and a valid `FullRebuild` succeeds without the historical node ID.
- [x] 4.7 Add negative structural coverage proving invalid schema, corpus paths, internal references, topology, rename/delete cleanup, or ghost nodes still block publication.
- [x] 4.8 Remove tests that treat fixed application symbols or concrete application edges as mandatory publication probes.
- [x] 4.9 Remove tests that treat ordinary feature `A -> V -> R -> E -> G` lineage, verification metadata blocks, or Graphify evidence JSON as supported workflow.
- [x] 4.10 Verify the wrapper does not require or inspect archived OpenSpec changes, `verification-report.md`, or `openspec/validation/*.post-archive.json`.
- [x] 4.11 Verify all four canonical artifacts are present and valid in the resulting tree.
- [x] 4.12 Verify `S..G` is non-empty and contains only a non-empty subset of the four allowlisted paths.
- [x] 4.13 Verify unchanged canonical artifacts are not artificially modified and an all-identical rebuild does not fabricate a graph commit.
- [x] 4.14 Preserve coverage for canonical encoding, JSON validity, nonzero counts, hashes, sensitive-data/path scans, and safe publication.
- [x] 4.15 Preserve incremental rename/delete, ghost-node, topology, and fallback-to-full-rebuild coverage.
- [x] 4.16 Verify a failed refresh leaves the previously accepted baseline unchanged.

## 5. Architecture validation

- [ ] 5.1 Fetch the published remote branch and record the exact remote architecture HEAD and exact change base before validation.
- [ ] 5.2 Create or use a clean checkout whose `HEAD` exactly equals the published remote architecture HEAD and record clean `git status` evidence.
- [ ] 5.3 Run `git diff --check` and record its exit code and result.
- [ ] 5.4 Run `.\openspec.cmd validate decouple-project-graph-refresh --strict` using the repository-local wrapper and record its exit code and exact passed/failed result.
- [ ] 5.5 Run `.\openspec.cmd validate --all --strict` using the repository-local wrapper and record its exit code and exact passed/failed counts.
- [ ] 5.6 Verify the architecture diff contains only the declared active OpenSpec change artifacts and no production code, tests, validation evidence, archives, root specs, or `graphify-out/` mutations.
- [ ] 5.7 Do not issue architecture `APPROVE` until all blocking findings are resolved and tasks 5.1-5.6 pass on the current published remote HEAD.

## 6. Implementation validation

- [x] 6.1 Run focused Graphify workflow tests.
- [x] 6.2 Run the full offline Python test suite.
- [x] 6.3 Run `git diff --check`.
- [x] 6.4 Run `.\openspec.cmd validate decouple-project-graph-refresh --strict`.
- [x] 6.5 Run `.\openspec.cmd validate --all --strict`.
- [x] 6.6 Verify no application production code, unrelated tests, validation evidence, archived changes, or `graphify-out/` artifacts changed.
- [x] 6.7 Commit and push the implementation before requesting independent validation.

## 7. Independent validation, archive, and merge

- [ ] 7.1 Independently validate the current published remote implementation HEAD in a clean detached worktree.
- [ ] 7.2 Require passing focused tests, full offline tests, strict change validation, strict all validation, repository-protection checks, and `git diff --check`.
- [ ] 7.3 Archive only after `APPROVE` or `APPROVE WITH NON-BLOCKING NOTES`.
- [ ] 7.4 Run ordinary post-archive strict validation, full offline tests, archive/root-spec diff review, and repository-protection checks.
- [ ] 7.5 Merge without any Graphify refresh, evidence JSON, report-only commit, evidence-only commit, graph-only commit, or stale-graph exception.
- [ ] 7.6 Schedule a separate graph maintenance refresh later only by explicit architect decision.
