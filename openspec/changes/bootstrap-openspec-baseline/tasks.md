## 1. Bootstrap OpenSpec

- [x] 1.1 Initialize the repository with OpenSpec 1.6.0 and the standard `spec-driven` schema.
- [x] 1.2 Record durable project context, workflow rules, and real validation commands.

## 2. Establish the baseline

- [x] 2.1 Verify the entry point, main window, screens, workers, handlers, parsers, and offline tests.
- [x] 2.2 Create capability specs for shell behavior, devices, lifecycle, and secure observability.
- [x] 2.3 Mark the prior reverse-engineered specification as historical source material.

## 3. Validate and finalize

- [x] 3.1 Run strict OpenSpec validation for the bootstrap artifacts and resulting normative specs.
- [x] 3.2 Run the offline test suite and documentation/path/secret checks.
- [ ] 3.3 Archive the bootstrap only after the checks pass, preserving the archived artifacts.
- [x] 3.4 Add a repository-local Windows OpenSpec wrapper that resolves the pinned project-local tool and forwards arguments and exit codes; verify it from a clean checkout.
